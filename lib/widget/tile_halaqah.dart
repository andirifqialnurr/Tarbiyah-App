import 'package:flutter/material.dart';

class HalaqahTile extends StatelessWidget {
  const HalaqahTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
